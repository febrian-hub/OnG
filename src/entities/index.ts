/**
 * Entities Layer Index
 *
 * Export all business entities from this file.
 * Each entity represents a business object with its own:
 * - model/ (types, schemas)
 * - ui/ (display components)
 */

// Add entity exports here as they are created
// Example: export * from './user';
