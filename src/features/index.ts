/**
 * Features Layer Index
 *
 * Export all feature modules from this file.
 * Each feature should be a self-contained module with its own:
 * - api/ (API calls)
 * - model/ (state, types)
 * - ui/ (components)
 */

// Add feature exports here as they are created
// Example: export * from './auth';
